第一關自問  
一份「清單」在 HTML 裡最適合用哪個標籤來表示？為什麼不是一堆 `<div>`？  
A: `<ul><li>` 比較好控制  
「輸入框 + 送出按鈕」這個組合，HTML 有沒有專門的標籤來包它？用它有什麼好處?  
A: 我是用 input+button，目前知道在form裡面寫button，會默認送出表單  
「已完成」這個狀態，你打算怎麼在 HTML 上表現？是換一個標籤，還是加一個 class？兩者差在哪？  
A:我是選擇新增class  
